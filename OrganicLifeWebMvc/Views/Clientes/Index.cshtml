@model IEnumerable<OrganicLifeWebMvc.Models.Cliente>

@{
    ViewData["Title"] = "Index";
}

<div style="padding:10px;">
    <h2 style="text-align:center;">Listagem de Clientes</h2>
    <p>
        <a asp-action="Create" class="btn btn-success">Adicionar Cliente</a>
    </p>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>
                Nome
            </th>
            <th>
                E-mail
            </th>
            <th>
                Cpf
            </th>
            <th>
                Celular
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Pessoa.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Pessoa.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Pessoa.Cpf)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Pessoa.Celular)
                </td>
                <td>
                    <a asp-action="Edit" class="btn btn-primary btn-sm p" asp-route-id="@item.Id" style="">Editar</a> |
                    <a asp-action="Details" class="btn btn-info btn-sm" asp-route-id="@item.Id">Detalhes</a> |
                    <a asp-action="Delete" class="btn btn-danger btn-sm" asp-route-id="@item.Id">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>