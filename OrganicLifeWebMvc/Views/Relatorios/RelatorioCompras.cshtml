@model IEnumerable<OrganicLifeWebMvc.Models.Venda>

@{
    ViewData["Title"] = "RelatorioCompras";
}

<h1>Minhas Compras</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th>
                Nome Fantasia
            </th>
            <th>
                Valor Total
            </th>
            <th>
                Desconto
            </th>
            <th>
                Taxa de Entrega
            </th>
            <th>
                Forma de Pagamento
            </th>
            <th>
                Status
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Fornecedor.PessoaJuridica.NomeFantasia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorTotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ValorDesconto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TaxaEntrega)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MeioPagamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pago)
            </td>
        </tr>
}
    </tbody>
</table>
